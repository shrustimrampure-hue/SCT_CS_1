# 👑 Caesar Cipher Program
A classic substitution cipher implemented in Python for basic text encryption and decryption.
# 🔑 Features
# Dual Mode: 
Supports both encryption (e) and decryption (d).
# Alphabet Wraparound: 
Correctly handles letter shifts that go past 'z' or before 'a'.
# Non-Alphabetic Preservation: 
Keeps spaces, punctuation, and numbers unchanged.
# 🚀 How to Run
# Prerequisite: 
Ensure Python is installed.
# Execution: 
Run the script file (e.g., caesar_cipher.py) using the Python interpreter.
# Interaction: 
The program will prompt you to:
. Select the mode (e or d).
. Enter the key (a number from 1 to 26).
. Provide the text to be processed.
